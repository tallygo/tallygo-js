// import WebSocket from '../src/wsClient'

describe('WebSocket', () => {
  // TODO: we need to mock at least part of the websocket
  // object before we can test
  // const webSocket = new WebSocket('ws://localhost:3200')

  it('has the expected url, path and method', () => {
    // expect(webSocket.url).toEqual('ws://localhost:3200')
    expect(1 + 1).toEqual(2)
  })
})
